apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-dgpj-backend.fullname" . }}-test-connection"
  labels:
    {{- include "helm-dgpj-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-dgpj-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
