apiVersion: v1
kind: Service
metadata:
  name: {{ include "dgpj-datastore.fullname" . }}
  labels:
    {{- include "dgpj-datastore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "dgpj-datastore.selectorLabels" . | nindent 4 }}
