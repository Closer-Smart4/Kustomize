apiVersion: v1
kind: Service
metadata:
  name: {{ include "dgpj-llm-microservice.fullname" . }}
  labels:
    {{- include "dgpj-llm-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "dgpj-llm-microservice.selectorLabels" . | nindent 4 }}
