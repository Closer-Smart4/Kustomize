apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dgpj-llm-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "dgpj-llm-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dgpj-llm-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
