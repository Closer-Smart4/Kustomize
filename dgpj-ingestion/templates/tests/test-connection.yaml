apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dgpj-ingestion.fullname" . }}-test-connection"
  labels:
    {{- include "dgpj-ingestion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dgpj-ingestion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
