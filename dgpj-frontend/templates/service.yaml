apiVersion: v1
kind: Service
metadata:
  name: {{ include "dgpj-frontend.fullname" . }}
  labels:
    {{- include "dgpj-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8501
      protocol: TCP
      name: http
  selector:
    {{- include "dgpj-frontend.selectorLabels" . | nindent 4 }}
