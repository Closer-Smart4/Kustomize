apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dgpj-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "dgpj-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dgpj-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
