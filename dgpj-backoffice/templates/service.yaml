apiVersion: v1
kind: Service
metadata:
  name: {{ include "dgpj-backoffice.fullname" . }}
  labels:
    {{- include "dgpj-backoffice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8501
      protocol: TCP
      name: http
  selector:
    {{- include "dgpj-backoffice.selectorLabels" . | nindent 4 }}
