apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-dgpj-frontend.fullname" . }}
  labels:
    {{- include "helm-dgpj-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8501
      protocol: TCP
      name: http
  selector:
    {{- include "helm-dgpj-frontend.selectorLabels" . | nindent 4 }}
