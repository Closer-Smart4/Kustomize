apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-dgpj-backend.fullname" . }}
  labels:
    {{- include "helm-dgpj-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "helm-dgpj-backend.selectorLabels" . | nindent 4 }}
